# 2일차 (8/11 화) — Web Search·LangGraph로 검색하고 흐름을 관리하는 에이전트 만들기



준비물 체크
- [ ] 어제 만든 `week6-agent` 프로젝트 (그대로 이어서 사용)
- [ ] 어제 산출물(Tool Use Agent, mem0 Memory) 정상 실행 확인
- [ ] Tavily API Key 준비 (Tavily는 [tavily.com](https://tavily.com)에서 무료 발급)
- [ ] Tavily 라이브러리 설치 (필요시 OpenAI 라이브러리 설치)
```bash
uv add tavily-python
uv add langchain_openai
```
---

## 세션 1 — Web Search, LangGraph 개념

### 1-1. Tavily — 실시간 Web Search

강의자료(RAG)에 없는 최신 정보(예: "이 논문 이후 후속 연구가 있나?")가 필요할 때,
Tavily API로 실시간 검색 결과를 Tool처럼 호출합니다.

### 1-2. LangGraph — Node·Edge·State

| 개념 | 의미 |
|---|---|
| **State** | 그래프 전체가 공유하는 "현재 상태" (대화 메시지, 중간 결과 등을 담은 딕셔너리/Pydantic 모델) |
| **Node** | 한 단계의 작업 단위 (LLM 호출, Tool 실행 등) — 함수 하나 |
| **Edge** | Node와 Node를 잇는 화살표. 조건에 따라 다음 Node를 바꾸는 **조건 분기(conditional edge)**도 가능 |

> 3주차 LCEL(`prompt | model | parser`)이 "한 줄로 흐르는 파이프"였다면, LangGraph는
> **"갈림길과 되돌아가는 길이 있는 흐름도"**입니다. Tool을 쓸지 말지, 몇 번 반복할지를
> 그래프가 스스로 판단합니다.

---

## 세션 2  — 실습 A: Search + Tool 구현

### A-1. Tavily Tool 연동 — `search_tool.ipynb`

어제(8/10) A-1에서 만든 `tool_basics.ipynb`와 같은 방식으로, 이번에도 **노트북**에서 셀 단위로
Tavily 검색 결과를 뜯어봅니다. 목표는 단순히 검색이 되는지 확인하는 것을 넘어, **검색
결과에서 어디까지 받아올지(제목·URL만 받을지, 원문 전체까지 받을지)에 따라 정보량과
비용이 어떻게 달라지는지**를 직접 비교하는 것입니다.

> 💡 **왜 이 비교가 중요한가**: 다음 실습의 `web_search` 도구는 `r['content'][:200]`
> 처럼 짧은 스니펫만 잘라 씁니다. 이건 실수가 아니라 **의도적인 절약**입니다. 검색 결과의
> 원문 전체를 매번 모델에 넘기면 정확도는 오를 수 있지만 토큰(비용)이 크게 늘어납니다.
> 언제 스니펫으로 충분하고 언제 원문이 필요한지 감을 잡는 것이 이번 실습의 핵심입니다.

**노트북 준비**: 어제(8/10) 세션 2(E-3)에서 설정한 커널을 그대로 사용합니다. `week6-agent` 폴더에
`search_tool.ipynb`라는 새 노트북 파일을 만들고, `Select Kernel`에서 같은
`week6-agent/.venv`가 선택되어 있는지만 확인하세요.

```bash
uv add tavily-python
```

**셀 1 — 클라이언트 준비**

```python
import os
from dotenv import load_dotenv
from tavily import TavilyClient

load_dotenv()
client = TavilyClient(api_key=os.environ["TAVILY_API_KEY"])
```

**셀 2 — 기본 검색 (title·url·content 스니펫만)**

```python
response = client.search("2026년 생성형 AI 교육 활용 사례", max_results=3)
response
```

**셀 3 — 기본 결과에는 어떤 키가 들어있는지 확인**

```python
print(response["results"][0].keys())
```

`title`, `url`, `content`, `score` 정도의 키가 보일 것입니다. 아직 `raw_content`
(원문 전체)는 요청하지 않았으므로 없거나 `None`입니다.

**셀 4 — title·url·스니펫 길이 출력 (원래 코드와 동일한 목적)**

```python
for r in response["results"]:
    print(r["title"], "-", r["url"])
    print("스니펫(content) 길이:", len(r["content"]), "자")
    print("-" * 30)
```

**셀 5 — 이번엔 원문(raw_content)까지 포함해서 재검색**

```python
response_raw = client.search(
    "2026년 생성형 AI 교육 활용 사례",
    max_results=3,
    include_raw_content=True,
)
print(response_raw["results"][0].keys())
```

`include_raw_content=True`를 주면 각 결과에 `raw_content`(해당 페이지의 원문 텍스트)가
추가로 채워집니다.

**셀 6 — 스니펫 vs 원문, 글자 수로 직접 비교**

```python
for basic, raw in zip(response["results"], response_raw["results"]):
    snippet_len = len(basic["content"])
    raw_len = len(raw.get("raw_content") or "")
    print(f"{basic['title'][:30]}...")
    print(f"  스니펫(content): {snippet_len}자 / 원문(raw_content): {raw_len}자")
```

**셀 7 — 실제 텍스트 내용을 나란히 미리보기**

```python
first = response_raw["results"][0]
print("=== 스니펫(content) ===")
print(first["content"][:300])
print("\n=== 원문(raw_content) 앞부분 300자 ===")
print((first.get("raw_content") or "원문 없음")[:300])
```

**셀 8 — 대략적인 토큰(비용) 차이 가늠해보기**

```python
def rough_tokens(text: str) -> int:
    # 정확한 토큰 수는 아니지만, 글자 수 대비 대략적인 규모를 가늠하는 용도입니다.
    return len(text) // 2

snippet_tokens = sum(rough_tokens(r["content"]) for r in response["results"])
raw_tokens = sum(rough_tokens(r.get("raw_content") or "") for r in response_raw["results"])

print(f"스니펫만 사용 시 대략 토큰: {snippet_tokens}")
print(f"원문까지 포함 시 대략 토큰: {raw_tokens}")
print(f"약 {raw_tokens / max(snippet_tokens, 1):.1f}배 차이")
```

**[체크포인트]**
- [ ] 기본 검색(`response`)에는 `raw_content`가 없거나 비어 있는 것을 확인했나요?
- [ ] `include_raw_content=True`로 재검색(`response_raw`)하면 `raw_content`가 실제로
      채워지나요?
- [ ] 스니펫과 원문의 글자 수 차이를 눈으로 확인했나요? (보통 원문이 몇 배 이상 깁니다)
- [ ] 원문을 포함했을 때 대략적인 토큰 수가 얼마나 늘어나는지 계산해 보았나요?

**[도전 과제]**
1. 검색 주제를 본인 강의·연구 분야와 관련된 것으로 바꿔서 같은 비교(셀 2~8)를 반복해
   보세요. 주제에 따라 스니펫만으로 충분한지, 원문이 꼭 필요한지 판단이 달라질 수 있습니다.
2. "이 검색을 Researcher 역할에 쓴다면, 어떤 상황에서 `include_raw_content=True`를
   켜야 할까?"를 한 문장으로 정리해 보세요. (힌트: 정확한 수치·인용이 필요한 연구 조사
   vs 개요 파악이면 충분한 일반 질문)

> 이 비교는 6주차 후반의 Multi-Agent 실습에서 Researcher 역할을 설계할 때 그대로
> 쓰입니다 — 모든 검색에 원문을 다 가져오는 대신, 필요할 때만 `include_raw_content`를
> 켜는 판단 자체가 Researcher의 역할 중 하나가 됩니다.



---
### A-2. Tavily 검색 Tool의 다양한 인자 사용 - 

```python
import os
from dotenv import load_dotenv
from langchain_core.tools import tool
from tavily import TavilyClient

load_dotenv()

tavily = TavilyClient(api_key=os.environ["TAVILY_API_KEY"])

@tool
def web_search(query: str) -> str:
    """최근 정보나 내부 강의자료에 없는 내용을 웹에서 검색한다."""
    try:
        response = tavily.search(
            query=query,
            search_depth="advanced",
            max_results=3,
            include_answer=False,
        )
        results = response.get("results", [])

        if not results:
            return "검색 결과가 없습니다. 질의를 더 구체적으로 바꾸거나 내부 자료를 확인하세요."

        lines = []
        for index, item in enumerate(results, start=1):
            title = item.get("title", "제목 없음")
            url = item.get("url", "URL 없음")
            content = item.get("content", "")[:300]
            lines.append(f"[{index}] {title}\nURL: {url}\n요약: {content}")

        return "\n\n".join(lines)

    except Exception as error:
        return f"웹 검색에 실패했습니다: {type(error).__name__}: {error}"
```

```bash
uv run python search_agent.py
```
**[도전 과제]**
1. 위 툴을 사용하는 에이전트를 만들어보세요.
2. 검색 결과가 화면에 빨리 나타나도록 스트림모드로도 바꿔보세요. 
3. 자료 검색에서 내 문서의 내용을 반영하는 RAG와의 결합 방법을 생각해보세요.

> 권장 우선순위:

```text
내부 자료로 답할 수 있는가?
 ├─ 예 → RAG
 └─ 아니오 → 최신 정보가 필요한가?
             ├─ 예 → Tavily Web Search
             └─ 아니오 → 일반 LLM 또는 사용자에게 추가 정보 요청
```

- **내부 근거 우선**: 강의자료·연구실 문서의 질문은 먼저 RAG를 사용한다.
- **최신성 판단**: 날짜, 최신, 현재, 최근, 개정, 발표, 후속 연구가 포함되면 웹 검색을 검토한다.
- **출처 표시**: 웹 검색을 사용한 답변에는 제목과 URL을 남긴다.
- **충돌 처리**: 내부 자료와 웹 자료가 다르면 어느 자료가 더 최근인지 확인하고 충돌 사실을 밝힌다.
- **검색 실패 처리**: 결과가 없거나 API 호출이 실패해도 추측으로 채우지 않는다.
- **비용 통제**: `max_results`, `search_depth`, 재시도 횟수를 제한하여 호출량을 관리한다.

> 참고 자료: Tavily 공식 문서의 [*Quickstart*](https://docs.tavily.com/), [*Python SDK Reference*](https://docs.tavily.com/sdk/python/reference), *Search API Reference*,
> *API Key Management*를 기준으로 작성

---
## 세션 2  — 실습 B: LangGraph

### B-1. LangGraph 첫 그래프 — `simple_graph.ipynb`

지금까지 State·Node·Edge를 개념으로만 배웠습니다. 이번 노트북에서는 아주 단순한
예제(농담 만들기 → 시 짓기 병렬 확장)로 LangGraph를 **손으로 직접** 조립해 봅니다.
도구(Tool)나 조건 분기 없이, 그래프의 가장 기본 골격 — Node를 만들고, Edge로 잇고,
그래프를 그려보고, 실행하는 것 — 에만 집중합니다. `week6-agent` 폴더에
`simple_graph.ipynb` 노트북을 만들고 아래 셀을 순서대로 실행하세요.

#### 1. 한국어 topic으로 농담 만들기 — `generate_joke(topic)`

**셀 1 — 모델 준비**

```python
from typing import TypedDict
from langchain.chat_models import init_chat_model
from langgraph.graph import StateGraph, START, END

MODEL = "anthropic:claude-sonnet-4-6"  # 모델 선택은 1일차 A-1 박스 참고
model = init_chat_model(MODEL, temperature=0.7)
```

**셀 2 — State 정의**

```python
class JokeState(TypedDict):
    topic: str
    joke: str
```

**셀 3 — Node 함수 정의**

```python
def generate_joke(state: JokeState) -> dict:
    topic = state["topic"]
    response = model.invoke(f"'{topic}'을(를) 주제로 한국어 아재 개그를 하나만 만들어줘. 농담만 출력해.")
    return {"joke": response.content}
```

**셀 4 — 그래프 구성: `START → generate_joke → END`**

```python
joke_builder = StateGraph(JokeState)
joke_builder.add_node("generate_joke", generate_joke)
joke_builder.add_edge(START, "generate_joke")
joke_builder.add_edge("generate_joke", END)

joke_graph = joke_builder.compile()
```

**셀 5 — 그래프 시각화**

```python
from IPython.display import Image, display
display(Image(joke_graph.get_graph().draw_mermaid_png()))
```

**셀 6 — 실행**

```python
result = joke_graph.invoke({"topic": "가을"})
print(result["joke"])
```

**[체크포인트]**
- [ ] 셀 5의 그래프 그림이 `__start__ → generate_joke → __end__` 모양으로 나오나요?
- [ ] `topic`을 "월요일 출근길"처럼 다른 값으로 바꿔 다시 실행해도 잘 동작하나요?

#### 2. 한국어 topic으로 시 짓기 — `generate_poem(topic)` 병렬 확장

이제 `generate_poem` 노드를 추가하고, `generate_joke`와 **병렬(동시에 각자 실행되고,
둘 다 끝나면 하나로 합쳐지는 구조)**로 연결합니다.

**셀 7 — State 확장 (joke·poem·combined 키 추가)**

```python
class CreativeState(TypedDict):
    topic: str
    joke: str
    poem: str
    combined: str
```

**셀 8 — 두 Node 함수 (joke는 그대로, poem을 새로 추가)**

```python
def generate_joke(state: CreativeState) -> dict:
    topic = state["topic"]
    response = model.invoke(f"'{topic}'을(를) 주제로 한국어 아재 개그를 하나만 만들어줘. 농담만 출력해.")
    return {"joke": response.content}

def generate_poem(state: CreativeState) -> dict:
    topic = state["topic"]
    response = model.invoke(f"'{topic}'을(를) 주제로 한국어로 짧은 시 한 편을 써줘. 시만 출력해.")
    return {"poem": response.content}
```

**셀 9 — 두 결과를 하나로 합치는 Node**

```python
def combine(state: CreativeState) -> dict:
    combined = (
        f"=== '{state['topic']}'에 대한 농담 ===\n{state['joke']}\n\n"
        f"=== '{state['topic']}'에 대한 시 ===\n{state['poem']}"
    )
    return {"combined": combined}
```

**셀 10 — 병렬 그래프 구성: `START`가 두 갈래로 갈라졌다가 `combine`에서 합쳐짐**

```python
builder = StateGraph(CreativeState)
builder.add_node("generate_joke", generate_joke)
builder.add_node("generate_poem", generate_poem)
builder.add_node("combine", combine)

builder.add_edge(START, "generate_joke")
builder.add_edge(START, "generate_poem")
builder.add_edge("generate_joke", "combine")
builder.add_edge("generate_poem", "combine")
builder.add_edge("combine", END)

creative_graph = builder.compile()
```

**셀 11 — 그래프 시각화**

```python
display(Image(creative_graph.get_graph().draw_mermaid_png()))
```

`__start__`에서 화살표가 `generate_joke`와 `generate_poem` **두 갈래로 갈라졌다가**,
`combine` 하나로 다시 모이는 모양이 보여야 합니다 — 3주차 LCEL의 일직선 파이프로는
그릴 수 없었던 구조입니다.

**셀 12 — 실행**

```python
result = creative_graph.invoke({"topic": "가을"})
print(result["combined"])
```

**[체크포인트]**
- [ ] 셀 11의 그래프에서 `generate_joke`와 `generate_poem`이 나란히(병렬로) 그려지고,
      둘 다 `combine`으로 모이나요?
- [ ] `combine` 함수 안에서 `state["joke"]`와 `state["poem"]`이 **둘 다** 이미 채워져
      있나요? (한쪽 노드만 먼저 끝났다고 `combine`이 실행되지 않고, 둘 다 끝날 때까지
      기다린다는 뜻입니다)
- [ ] `result["combined"]`에 농담과 시가 모두 출력되나요?

**[도전 과제]**
1. `generate_joke`와 `generate_poem` 함수 맨 앞에 `print(f"{topic} 시작")`을 하나씩
   추가해, 어떤 노드가 먼저 끝나든 `combine`은 항상 **둘 다 끝난 뒤**에만 실행된다는 것을
   출력 순서로 확인해 보세요.
2. `generate_slogan(topic)`(예: 광고 카피 한 줄) 노드를 하나 더 추가해, `generate_joke`·
   `generate_poem`·`generate_slogan` **세 갈래**가 `combine`으로 모이는 구조로 확장해
   보세요. `combine` 함수도 세 결과를 모두 담도록 고쳐야 합니다.

> 💡 **오늘 이 실습이 왜 중요한가**: `generate_joke`·`generate_poem`처럼 서로 무관한
> 작업을 **동시에 갈래로 나눠 처리하고 나중에 합치는 구조**는, `web_search` +
> `calculator` Tool 호출이나 내일(8/12) 배울 Multi-Agent의 Planner·Researcher 병렬
> 처리와 정확히 같은 그래프 패턴입니다. 오늘은 도구도, 조건 분기도 없는 **가장 단순한
> 뼈대**로 이 패턴만 연습해 두는 것입니다.

> **참고(오프라인 환경)**: 셀 5·11의 `draw_mermaid_png()`는 그림을 그리기 위해
> 외부 서비스(mermaid.ink)에 접속합니다. 실습실 네트워크가 막혀 있어 오류가 난다면,
> `uv add grandalf` 후 `print(joke_graph.get_graph().draw_ascii())`처럼 텍스트
> 기반 그림으로 대체할 수 있습니다.

### B-2. LangGraph로 Tool + Search를 엮기 — `agent_graph.py`

```python
# agent_graph.py
import os
from dotenv import load_dotenv
from typing import Annotated, TypedDict
from langchain.chat_models import init_chat_model
from langchain_core.tools import tool
from langchain_core.messages import HumanMessage
from langgraph.graph import StateGraph, END
from langgraph.graph.message import add_messages
from langgraph.prebuilt import ToolNode, tools_condition
from tavily import TavilyClient

load_dotenv()
tavily = TavilyClient(api_key=os.environ["TAVILY_API_KEY"])

@tool
def web_search(query: str) -> str:
    """최신 정보나 강의자료에 없는 내용을 실시간으로 검색한다."""
    results = tavily.search(query, max_results=3)
    return "\n".join(f"- {r['title']}: {r['content'][:200]}" for r in results["results"])

@tool
def calculator(expression: str) -> str:
    """사칙연산 수식을 계산한다."""
    try:
        return str(eval(expression, {"__builtins__": {}}))
    except Exception as e:
        return f"계산 오류: {e}"

tools = [web_search, calculator]
MODEL = "anthropic:claude-sonnet-4-6"  # 모델 선택은 1일차 A-1 박스 참고
model = init_chat_model(MODEL, temperature=0).bind_tools(tools)

class State(TypedDict):
    messages: Annotated[list, add_messages]

def call_model(state: State):
    return {"messages": [model.invoke(state["messages"])]}

graph_builder = StateGraph(State)
graph_builder.add_node("agent", call_model)
graph_builder.add_node("tools", ToolNode(tools))
graph_builder.set_entry_point("agent")
graph_builder.add_conditional_edges("agent", tools_condition)  # Tool이 필요하면 tools로, 아니면 END로
graph_builder.add_edge("tools", "agent")  # Tool 실행 후 다시 agent로 돌아와 최종 답변 생성

app = graph_builder.compile()

result = app.invoke({
    "messages": [HumanMessage(content="현재 원-달러 환율을 검색해서 알려주고, 그 값에 1000을 곱해줘")]
})
print(result["messages"][-1].content)
```

```bash
uv run python agent_graph.py
```

**[체크포인트]**
- [ ] 그래프가 `web_search`를 먼저 호출하고, 그 결과로 `calculator`를 또 호출했나요?
      (하나의 질문에 **두 번의 Tool 호출**이 연쇄로 일어나는 것 — 이게 LangGraph의 힘입니다)
- [ ] Tool 없이 답하려고 했다면 → `tools_condition`이 제대로 붙었는지 확인하세요.

---
### B-3. LangGraph 메모리 체크포인터 기능  — `langgraph_memory.ipynb`
**셀 1 — 실행**
```python
import os
from dotenv import load_dotenv
from typing import Annotated, TypedDict
from langchain.chat_models import init_chat_model
from langchain_core.tools import tool
from langchain_core.messages import HumanMessage
from langgraph.graph import StateGraph, END
from langgraph.graph.message import add_messages
from langgraph.prebuilt import ToolNode, tools_condition
from tavily import TavilyClient

load_dotenv()
tavily = TavilyClient(api_key=os.environ["TAVILY_API_KEY"])

@tool
def web_search(query: str) -> str:
    """최신 정보나 강의자료에 없는 내용을 실시간으로 검색한다."""
    results = tavily.search(query, max_results=3)
    return "\n".join(f"- {r['title']}: {r['content'][:200]}" for r in results["results"])
@tool
def calculator(expression: str) -> str:
    """사칙연산 수식을 계산한다."""
    try:
        return str(eval(expression, {"__builtins__": {}}))
    except Exception as e:
        return f"계산 오류: {e}"

tools = [web_search, calculator]
MODEL = "anthropic:claude-sonnet-4-6"  # 모델 선택은 1일차 A-1 박스 참고
model = init_chat_model(MODEL, temperature=0).bind_tools(tools)

class State(TypedDict):
    messages: Annotated[list, add_messages]

def call_model(state: State):
    return {"messages": [model.invoke(state["messages"])]}

graph_builder = StateGraph(State)
graph_builder.add_node("agent", call_model)
graph_builder.add_node("tools", ToolNode(tools))
graph_builder.set_entry_point("agent")
graph_builder.add_conditional_edges("agent", tools_condition)  # Tool이 필요하면 tools로, 아니면 END로
graph_builder.add_edge("tools", "agent")  # Tool 실행 후 다시 agent로 돌아와 최종 답변 생성
```
**셀 2 — 실행**
```python
from langgraph.checkpoint.memory import MemorySaver
memory = MemorySaver()
graph = graph_builder.compile(checkpointer=memory)
```
**셀 3 — 실행**
```python
config = {"configurable": {"thread_id": "1"}}
user_input = "안녕하세요! 제 이름은 랭체인입니다."

events = graph.stream(
    {"messages": [{"role": "user", "content": user_input}]},
    config,
    stream_mode="values"
)

for event in events:
    event["messages"][-1].pretty_print()
```
**셀 4 — 실행**
```python
user_input = "내 이름이 뭐였나요?"

events = graph.stream(
    {"messages": [{"role": "user", "content": user_input}]},
    config,
    stream_mode="values"
)

for event in events:
    event["messages"][-1].pretty_print()
```

**셀 5 — 실행**
```python
snapshot = graph.get_state(config)
print(snapshot)
```

**셀 5 — 실행**
```python
snapshot.next  # 다음 실행될 노드 (대화가 끝났다면 빈 튜플)
```

**셀 6 — 실행**
```python
snapshot.values  # 현재까지의 메시지 리스트
```
---
### B-4. LangGraph 라우터  — `langgraph_router.py`
```python
# 라우팅

from typing_extensions import Literal, TypedDict
from langchain_core.messages import HumanMessage, SystemMessage
from pydantic import BaseModel, Field
from dotenv import load_dotenv
from langchain.chat_models import init_chat_model
from langgraph.graph import StateGraph
from IPython.display import Image, display

load_dotenv()

llm = init_chat_model("anthropic:claude-sonnet-4-6") 
# 1) 라우팅 결정을 위한 구조화 출력 스키마
class Route(BaseModel):
    step: Literal["poem", "story", "joke"] = Field(
        None, description="라우팅의 다음 단계"
    )

# 2) 구조화 출력으로 동작하는 라우터 LLM
router = llm.with_structured_output(Route)

# 3) 상태 정의
class State(TypedDict):
    input: str
    decision: str
    output: str

# 4) 노드 정의 (각각 전용 프롬프트 사용)
def llm_call_1(state: State):
    """이야기 작성"""
    result = llm.invoke(f"다음 주제로 짧은 이야기를 작성해 주세요: {state['input']}")
    return {"output": result.content}

def llm_call_2(state: State):
    """농담 작성"""
    result = llm.invoke(f"다음 주제로 짧은 농담을 작성해 주세요: {state['input']}")
    return {"output": result.content}

def llm_call_3(state: State):
    """시 작성"""
    result = llm.invoke(f"다음 주제로 짧은 시를 작성해 주세요: {state['input']}")
    return {"output": result.content}

def llm_call_router(state: State):
    """입력을 적절한 노드로 라우팅"""
    decision = router.invoke(
        [
            SystemMessage(
                content="사용자 요청을 이야기(story), 농담(joke), 시(poem) 중 하나로 분류하고, "
                        "가장 알맞은 항목을 step에 지정하세요."
            ),
            HumanMessage(content=state["input"]),
        ]
    )
    return {"decision": decision.step}

# 5) 조건부 엣지에서 분기 로직
def route_decision(state: State):
    if state["decision"] == "story":
        return "llm_call_1"
    elif state["decision"] == "joke":
        return "llm_call_2"
    elif state["decision"] == "poem":
        return "llm_call_3"

# 6) 워크플로 구성
router_builder = StateGraph(State)
router_builder.add_node("llm_call_1", llm_call_1)
router_builder.add_node("llm_call_2", llm_call_2)
router_builder.add_node("llm_call_3", llm_call_3)
router_builder.add_node("llm_call_router", llm_call_router)

router_builder.add_edge(START, "llm_call_router")
router_builder.add_conditional_edges(
    "llm_call_router",
    route_decision,
    {
        "llm_call_1": "llm_call_1",
        "llm_call_2": "llm_call_2",
        "llm_call_3": "llm_call_3",
    },
)
router_builder.add_edge("llm_call_1", END)
router_builder.add_edge("llm_call_2", END)
router_builder.add_edge("llm_call_3", END)

router_workflow = router_builder.compile()

# 7) 다이어그램 확인
display(Image(router_workflow.get_graph().draw_mermaid_png()))

# 8) 실행
state = router_workflow.invoke({"input": "한국에 대한 농담을 써 주세요"})
print(state["output"])
```
---

## 오늘의 산출물 체크

- [ ] `search_tool.ipynb` — title/url 스니펫 vs raw_content(원문) 비교 결과
- [ ] `simple_agent.ipynb` — 농담·시 생성 노드의 직렬/병렬 그래프 구성 및 실행 결과
- [ ] `agent_graph.py` — Tool+Search를 엮은 LangGraph 실행 결과
- [ ] `langgraph_memory.ipynb` — LangGraph 메모리 기능
- [ ] `langgraph_router.py` — LangGraph 라우터
- [ ] 아래 템플릿으로 `W6_D2_이름.md` 작성

## 제출용 Markdown 템플릿

```markdown
# AI MASTER 6주차 2일차 — [이름 / 학과]

## 1. Web Search (Tavily)
- 검색한 질의:
- 결과 품질 평가(관련성):
- 스니펫(content) vs 원문(raw_content) 글자 수·토큰 비교 결과:
- 본인 에이전트에는 언제 raw_content가 필요하다고 판단했는가:

## 2. LangGraph
- (simple_agent.ipynb) 직렬 구조(START→generate_joke→END)와 병렬 구조(fan-out/fan-in)의 차이:
- Node 구성 (agent/tools 외 추가한 Node가 있다면):
- 조건 분기가 작동한 예시(질문과 결과):

## 3. 본인 LangGraph 적용 계획

```

---

## 자주 막히는 지점 (트러블슈팅)

- **`tools_condition`이 항상 END로 빠져요** → Tool의 docstring이 질문과 관련 있어 보이도록
  구체적으로 썼는지 확인하세요. "검색한다"보다 "최신 정보나 강의자료에 없는 내용을 검색한다"처럼
  **언제 쓰는지**를 명시해야 선택 확률이 올라갑니다.
- **`uv add`가 버전 충돌을 냅니다** → `uv add tavily-python`을 먼저 넣고 확인하세요.
  그래도 안 되면 `uv sync --upgrade`.
- **API 호출 비용이 걱정돼요** → 짧은 질문으로 먼저 흐름을 확인한 뒤, 정상 동작이 확인되면
  실제 강의자료로 넓혀가세요.
- **`jupyter lab`/그래프 시각화가 안 돼요** → `draw_mermaid_png()`는 외부 서비스(mermaid.ink)에
  접속합니다. 네트워크가 막혀 있다면 `uv add grandalf` 후 `draw_ascii()`로 대체하세요.
