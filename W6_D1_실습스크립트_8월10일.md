# 1일차 (8/10 월) — Tool Use·Function Calling과 Memory로 도구를 쓰고 기억하는 에이전트 만들기

> 오늘 하루가 끝나면: 3주차에 만든 AI 조교가 **"질문에 답만 하는 조교"에서 "필요할 때
> 계산기·검색 같은 도구를 스스로 불러 쓰고, 대화가 끝나도 사실을 기억하는 에이전트"**로
> 바뀝니다.

> 🔁 **지난 주 연결**: 5주차까지 만든 건 "물어보면 내 자료에서 찾아 답하는" RAG 에이전트였습니다.
> 오늘은 여기에 **Tool Use(도구 사용)**와 **Memory(mem0)**를 추가해, 계산·검색처럼 모델
> 혼자서는 정확히 할 수 없는 일을 Python 함수에 맡기고, 세션이 끝나도 남는 사실을 저장·
> 검색하는 법을 배웁니다. (실시간 웹 검색·LangGraph는 내일 8/11에 다룹니다.)

오늘 실습 파일은 **3개뿐**입니다. 도구는 노트북 하나에서 셀을 이어가며 쌓고, 마지막에
전부 실은 챗봇 스크립트로 마무리합니다.

| 단계 | 내용 | 파일 |
|---|---|---|
| A-3 | 계산기·단어수 Tool + `response` 구조 뜯어보기 | `tool_basics.ipynb` 시작 |
| A-4 | 시간 Tool 추가 (틀린 도구 → 고치기) | 같은 노트북 계속 |
| A-5 | 유튜브 검색·자막·요약 파이프라인 | `youtube_summary.ipynb` (새 노트북) |
| A-6 | **도구 4개 탑재** 멀티턴 챗봇 | `multiturn_chatbot.py` |

준비물 체크
- [ ] uv 설치 확인 (`uv --version`)
- [ ] Anthropic API Key 준비
- [ ] mem0 임베딩용 OpenAI API Key (또는 mem0 로컬 임베딩 대체 — 트러블슈팅 참고)
- [ ] Windows 개발자 모드를 On (Windows 설정 → 시스템 → 고급 → 개발자용 → 개발자 모드 켜기) - 개발자 모드를 켠 다음엔 주피터노트북이나 VS Code를 닫고 재시작 
---

## 세션 1 — Tool Use란 무엇인가

### 1-1. "본체는 계산을 못 한다"

LLM은 다음 토큰을 확률적으로 예측하는 모델이라, `12345 × 6789` 같은 계산을 시키면
**그럴듯하지만 틀린 답**을 내놓기 쉽습니다. Tool Use는 이 문제를 "모델이 직접 계산하게"
하는 대신 **"계산이 필요하면 계산기 함수를 부르게"** 만드는 방식입니다.

| 구분 | 방식 |
|---|---|
| Tool 없는 LLM | "12345×6789는 대략 83,810,205입니다" (틀릴 수 있음) |
| Tool Use LLM | "계산이 필요하군요" → `multiply(12345, 6789)` 함수 호출 → 정확한 값 반환 |

### 1-2. Function Calling 동작 구조

1. 우리가 Python 함수를 정의하고, 그 함수의 **이름·설명·파라미터**를 모델에 알려준다.
2. 사용자가 질문하면, 모델은 "이 함수를 이 인자로 불러야겠다"는 **요청**만 돌려준다
   (모델이 직접 함수를 실행하는 게 아니다).
3. 우리 코드가 그 함수를 **실제로 실행**하고, 결과를 다시 모델에게 돌려준다.
4. 모델이 그 결과를 바탕으로 최종 답변을 만든다.

> 💡 3주차에 배운 ReAct(Reason→Act→Observe) 루프가 바로 이 구조입니다. 오늘은 이걸
> 실제 Python 코드로 구현합니다.

---


## 세션 2 — 실습 A: Tool 기초

### A-1. uv 프로젝트 초기화

```bash
uv init week6-agent
cd week6-agent
uv add langchain langchain-anthropic langgraph python-dotenv pydantic rich
uv add tzdata ipywidgets
uv add "mem0ai[nlp]" fastembed python-dotenv ipykernel ipywidgets
uv pip install --python .venv\Scripts\python.exe pip
.venv\Scripts\python.exe -m spacy download en_core_web_sm
```

- `uv init`이 `pyproject.toml`을 만들고, `uv add`가 가상환경 생성 + 패키지 설치 +
  `uv.lock` 고정을 한 번에 처리합니다.
- 실행은 항상 `uv run python 파일명.py`로 합니다.

### A-2. `.env` 파일 만들기

```bash
# .env
ANTHROPIC_API_KEY=sk-ant-...
TAVILY_API_KEY=tvly-...
OPENAI_API_KEY=sk-...   # mem0 임베딩용 (트러블슈팅 참고 시 생략 가능)
```

### A-3. 계산기·단어수 Tool과 호출 구조 — `tool_basics.ipynb`

오늘 도구 실습은 **Jupyter Notebook 하나**(`tool_basics.ipynb`)에서 셀을 이어가며
진행합니다. 도구를 하나 추가할 때마다 같은 리듬을 반복합니다:

```text
① 셀 추가 → ② 도구(@tool) 정의 → ③ 새 이름으로 모델에 재바인딩 → ④ 프롬프트로 호출 테스트
```

이번 A-3에서는 첫 도구 2개(`calculator`, `word_count`)를 정의하고,
`model_with_tools.invoke(...)`가 돌려주는 `response` 객체(AIMessage)의 구조를 셀 단위로
직접 뜯어봅니다.

> 💡 왜 노트북인가: `.py` 스크립트는 실행하면 끝까지 한 번에 지나가 버리지만, 노트북은
> 셀마다 결과가 그대로 남아 있어 `response`, `response.tool_calls`, `response.content`를
> 각각 따로 찍어보며 **비교**하기에 좋습니다. 오늘 여기서 확인한 필드들(`tool_calls`,
> `content`, `response_metadata`, `usage_metadata`)이 A-4~A-6, 그리고 내일(8/11)
> LangGraph 실습의 루프 안에서 그대로 다시 쓰입니다.

**노트북 준비**

1. **ipykernel 설치** — 노트북에서 우리 프로젝트의 가상환경을 커널로 쓸 수 있게 해주는
   패키지입니다.

   ```bash
   uv add ipykernel jupyter
   ```

2. **VS Code에 Jupyter 확장 설치** — VS Code 좌측 사이드바의 Extensions 아이콘
   (또는 `Ctrl+Shift+X` / Mac은 `Cmd+Shift+X`)을 눌러 "Jupyter"(Microsoft 제공)를
   검색해 설치합니다. 이미 설치되어 있다면 이 단계는 건너뜁니다.

3. **노트북 파일 만들기** — `week6-agent` 폴더 안에 `tool_basics.ipynb`라는
   이름의 새 파일을 만듭니다. VS Code 탐색기에서 "새 파일" → 파일명 끝에 `.ipynb`를
   붙이면, VS Code가 자동으로 노트북 편집 화면으로 열어 줍니다.

4. **커널 선택** — 노트북을 열면 우측 상단에 **"Select Kernel"** 버튼이 보입니다.
   클릭 → **Python Environments** → 오늘 `uv init week6-agent`로 만들어진
   `week6-agent/.venv`를 선택합니다.
   - ⚠️ 이 단계를 건너뛰고 컴퓨터의 전역(global) Python이 선택된 채로 실행하면,
     `langchain-anthropic` 같은 패키지를 "찾을 수 없다"는 오류가 납니다 — 반드시
     오늘 만든 프로젝트의 `.venv`를 골라야 합니다.
   - 목록에 `.venv`가 안 보이면, VS Code 우측 상단 커맨드 팔레트(`Ctrl+Shift+P`)에서
     "Developer: Reload Window"를 실행해 다시 시도하거나, "Select Kernel" →
     "Enter interpreter path..."로 `week6-agent/.venv/bin/python` 경로를 직접
     입력하세요.

5. (선택) VS Code 대신 브라우저에서 Jupyter Lab을 쓰고 싶다면 터미널에서 아래처럼
   직접 실행할 수도 있습니다.

   ```bash
   uv run jupyter lab
   ```

커널까지 연결되었다면, 아래 셀들을 순서대로 하나씩 실행하세요.
**오늘 이 노트북은 A-4까지 계속 이어서 사용하므로 닫지 마세요.**

**[환경설정 체크포인트]**
- [ ] 노트북 우측 상단 커널 이름이 (전역 Python이 아니라) `week6-agent`의 `.venv`로 표시되나요?
- [ ] 셀 1(임포트 + 도구 정의)을 실행했을 때 `ModuleNotFoundError` 없이 통과하나요?

**셀 1 — 모델 준비와 첫 도구 2개 정의**

```python
import os
from dotenv import load_dotenv
from langchain_anthropic import ChatAnthropic
from langchain_core.tools import tool

load_dotenv()

@tool
def calculator(expression: str) -> str:
    """사칙연산 수식 문자열을 계산한다. 예: '12345 * 6789'"""
    try:
        # eval은 실습용 최소 예시입니다. 실전에서는 안전한 수식 파서를 쓰세요.
        result = eval(expression, {"__builtins__": {}})
        return str(result)
    except Exception as e:
        return f"계산 오류: {e}"

@tool
def word_count(text: str) -> str:
    """텍스트의 단어 수와 글자 수를 센다."""
    return f"단어 수: {len(text.split())}, 글자 수: {len(text)}"

model = ChatAnthropic(model="claude-sonnet-4-6", temperature=0)
model_with_tools = model.bind_tools([calculator, word_count])
```

**셀 2 — 호출하고, 변수만 두어 노트북이 자동으로 보여주게 하기**

```python
response = model_with_tools.invoke("12345 곱하기 6789는 얼마야?")
response
```

**셀 3 — 타입과 전체 구조를 사람이 읽기 좋게 확인**

```python
print(type(response))
response.pretty_print()
```

**셀 4 — 도구 호출시 AIMessage (`response`) 구조 보기**

```python
import json
data = response.model_dump()
print(json.dumps(data, indent=2, ensure_ascii=False))
```

도구 호출만 요청된 응답은 보통 `content`가 빈 문자열입니다. 모델이 "아직 최종 답을
만들지 않았다 — 먼저 도구 실행 결과를 받아야 한다"는 중간 상태이기 때문입니다.

**셀 5 — `tool_calls` 뜯어보기**

```python
for call in response.tool_calls:
    print("도구 이름:", call["name"])
    print("전달 인자:", call["args"])
    print("호출 ID:", call["id"])
    print("-" * 30)
```

`id`가 왜 필요한지 기억해 두세요 — 나중에 도구 실행 결과를 `ToolMessage`로 모델에게
돌려줄 때, 이 `id`로 "어느 호출에 대한 결과인지"를 짝지어 줍니다.

**셀 6 — `response_metadata`에서 멈춘 이유 확인**

```python
response.response_metadata
```

```python
print(response.response_metadata.get("stop_reason"))
```

`stop_reason`이 `"tool_use"`로 나오면, 모델이 "도구 호출이 필요해서 멈췄다"는 뜻입니다.
(도구 호출 없이 답이 끝났다면 이 값은 `"end_turn"`이 됩니다 — 셀 9에서 비교합니다.)

**셀 7 — `usage_metadata`로 토큰 수 확인**

```python
response.usage_metadata
```

`input_tokens`·`output_tokens`·`total_tokens`가 보이나요? 3주차에 배운 토큰 개념이
여기서 실제 숫자로 확인됩니다 — 호출 한 번에 얼마나 쓰이는지가 곧 비용입니다.

**셀 8 — 도구를 실제로 실행해 결과 얻기**

```python
call = response.tool_calls[0]
result = calculator.invoke(call["args"])
print("도구 실행 결과:", result)
```

**셀 9 — 도구가 필요 없는 질문과 비교**

```python
response2 = model_with_tools.invoke("Claude라는 이름은 어디서 왔어?")
print("tool_calls:", response2.tool_calls)
print("content:", repr(response2.content))
print("stop_reason:", response2.response_metadata.get("stop_reason"))
```

도구가 필요 없을 때는 `tool_calls`가 빈 리스트(`[]`)이고, `content`에 실제 답변
텍스트가 들어 있으며, `stop_reason`은 `"end_turn"`이어야 정상입니다. 셀 4~6과 나란히
비교해 보세요.

**[체크포인트]**
- [ ] 도구 호출이 있을 때(`response`)와 없을 때(`response2`)의 `content` 차이를 확인했나요?
- [ ] `tool_calls` 안의 `name`·`args`·`id` 세 필드가 각각 무엇을 의미하는지 설명할 수 있나요?
- [ ] `response_metadata["stop_reason"]`이 `tool_use`/`end_turn`으로 갈리는 것을 확인했나요?
- [ ] `usage_metadata`로 두 호출(`response`, `response2`)의 토큰 수를 비교해 보았나요?

**[도전 과제]**
1. `word_count`가 호출되도록 질문을 바꿔 같은 분석(셀 2~8)을 반복하고, `tool_calls`의
   `args` 구조가 `calculator`와 어떻게 다른지(파라미터 이름이 다름) 비교해 보세요.
2. 지금 등록된 도구 중 어느 것과도 맞지 않는 요청(예: "내 대신 이메일 보내줘")을 던져보고,
   `tool_calls`가 비는지, 모델이 어떻게 반응하는지 관찰해 보세요.
3. "12345 곱하기 6789, 그리고 1 더하기 9는 얼마야?" 로도 질문을 바꿔봅니다. 도구 호출은 몇 번 인가요?
> 결과 문구 자체는 호출할 때마다 조금씩 다를 수 있지만, `content`·`tool_calls`·
> `response_metadata`·`usage_metadata`라는 **필드 이름과 구조는 항상 동일**합니다.
> A-6와 내일(8/11) LangGraph 실습에서 도구 호출을 자동으로 반복시킬 때, 바로 이 구조
> (`ai_message.tool_calls`가 비어 있는지 여부)를 조건으로 사용합니다.

---

## 세션 3 — Tool 확장: 시간·유튜브, 그리고 멀티턴 챗봇

### A-4. 시간 Tool 추가 — 같은 노트북에 셀 이어서

**새 파일을 만들지 않습니다.** `tool_basics.ipynb`에 셀을 이어서, **시간대 정보가 빠진
도구가 어떻게 틀린 답을 만드는지** 직접 확인하고, 이를 고친 뒤 **한 질문에서 도구가 여러 번
호출되는 상황**까지 뜯어봅니다. A-3의 리듬 그대로입니다: 도구 추가 → **새 이름으로
재바인딩**(`model_with_tools2`, `model_with_tools3`) → 프롬프트 테스트.

#### 1단계 — 시간대를 모르는 Tool의 함정

**셀 10 — 시간대를 고려하지 않는 `get_current_time` 추가**

```python
from datetime import datetime

@tool
def get_current_time() -> str:
    """현재 시간을 알려준다."""
    return datetime.now().strftime("%Y-%m-%d %H:%M:%S")

# 참고: 이 함수가 실제로 어느 시간대를 반환하는지 먼저 확인해 둡니다.
import time
print("이 컴퓨터(서버)의 시간대:", time.tzname)
```

**셀 11 — 세 개의 Tool로 두 번째 모델 구성 (앞의 `model_with_tools`는 그대로 두고 새 이름)**

```python
model_with_tools2 = model.bind_tools([calculator, word_count, get_current_time])
```

**셀 12 — "뉴욕은 지금 몇 시야?"를 질문하고 도구 호출 확인하기**

```python
from langchain_core.messages import HumanMessage, ToolMessage

messages = [HumanMessage(content="뉴욕은 지금 몇 시야?")]

ai_message = model_with_tools2.invoke(messages)
messages.append(ai_message)

print("=== 모델이 요청한 도구 호출 ===")
print(ai_message.tool_calls)
```

**셀 13 — 끝까지 추적하기**

```python
# 요청된 도구를 실제로 실행
for call in ai_message.tool_calls:
    result = get_current_time.invoke(call["args"])
    print(f"\n실행한 도구: {call['name']}({call['args']}) -> {result}")
    messages.append(ToolMessage(content=str(result), tool_call_id=call["id"]))

final = model_with_tools2.invoke(messages)
print("\n=== 최종 답변 ===")
print(final.content)
```

**[체크포인트]** 최종 답변이 정말로 뉴욕(미국 동부) 시각인가요, 아니면 셀 10에서 확인한
**이 컴퓨터의 시간대**를 그대로 "뉴욕 시간"이라고 우기는 답인가요? `get_current_time`은
어느 도시를 물어보든 **항상 같은 값**을 반환합니다 — 도구 자체에 위치(시간대) 정보가
전혀 없기 때문에, 모델이 아무리 "뉴욕"이라는 단어를 이해하고 도구를 정확히 호출해도
**결과는 틀릴 수밖에 없습니다**. 도구가 부실하면 모델이 아무리 똑똑해도 소용없다는
것을 보여주는 사례입니다.

#### 2단계 — 시간대를 받는 Tool로 고치기

**셀 14 — `timezone` 인자를 받는 `get_current_time_with_timezone` 추가**

```python
from zoneinfo import ZoneInfo

@tool
def get_current_time_with_timezone(timezone: str) -> str:
    """IANA 시간대 이름(예: 'Asia/Seoul', 'America/New_York', 'Europe/London')을 받아
    해당 지역의 현재 시간을 알려준다."""
    try:
        now = datetime.now(ZoneInfo(timezone))
        return now.strftime("%Y-%m-%d %H:%M:%S %Z")
    except Exception as e:
        return f"시간대 오류: {e} (예: 'Asia/Seoul', 'America/New_York' 형식으로 입력)"
```

```bash
uv add tzdata
```

> `tzdata`는 운영체제에 IANA 시간대 데이터베이스가 없을 때(특히 Windows) `zoneinfo`가
> 참조할 시간대 정보를 보충해 줍니다. macOS/Linux에서도 설치해 두면 안전합니다.

**셀 15 — 세 번째 모델 구성 (`get_current_time`을 새 도구로 교체)**

```python
model_with_tools3 = model.bind_tools([calculator, word_count, get_current_time_with_timezone])
```

**셀 16 — 단일 도시 질문: "지금 뉴욕 몇 시?"**

```python
messages2 = [HumanMessage(content="지금 뉴욕 몇 시?")]

ai_message2 = model_with_tools3.invoke(messages2)
messages2.append(ai_message2)

print("=== 도구 호출 요청 ===")
print(ai_message2.tool_calls)
```

**[체크포인트]** `tool_calls`가 1개이고, `args`에 `{"timezone": "America/New_York"}`처럼
IANA 형식 문자열이 들어 있나요? 모델이 "뉴욕"이라는 한국어 단어를 도구 docstring의 예시
형식에 맞춰 **스스로 변환**했다는 점을 확인하세요.

**셀 17 — 실행하고 최종 답 받기**

```python
for call in ai_message2.tool_calls:
    result = get_current_time_with_timezone.invoke(call["args"])
    print(f"실행한 도구: {call['name']}({call['args']}) -> {result}")
    messages2.append(ToolMessage(content=str(result), tool_call_id=call["id"]))

final2 = model_with_tools3.invoke(messages2)
print("\n=== 최종 답변 ===")
print(final2.content)
```

이번에는 실제 뉴욕 시각(동부 표준시/서머타임 기준)이 답변에 반영되는지 확인하세요.

**셀 18 — 여러 도시를 한 번에 묻기: "지금 서울, 뉴욕 몇 시?"**

```python
messages3 = [HumanMessage(content="지금 서울, 뉴욕 몇 시?")]

ai_message3 = model_with_tools3.invoke(messages3)
messages3.append(ai_message3)

print("=== 이번 요청에는 도구 호출이 몇 개 들어있나요? ===")
print("호출 개수:", len(ai_message3.tool_calls))
for i, call in enumerate(ai_message3.tool_calls, start=1):
    print(f"\n[{i}번째 호출]")
    print("  도구 이름:", call["name"])
    print("  전달 인자:", call["args"])
    print("  호출 ID  :", call["id"])
```

**[체크포인트]** `ai_message3.tool_calls`의 길이가 **2**인가요? (서울용 호출 1개 + 뉴욕용
호출 1개) 두 호출의 `id`가 서로 다른가요? — 이 `id`가 있어야, 나중에 각 실행 결과를
"어느 질문에 대한 답인지" 헷갈리지 않고 정확히 짝지을 수 있습니다.

**셀 19 — 호출된 만큼 전부 실행하고, `id`로 결과를 짝지어 최종 답 받기**

```python
for call in ai_message3.tool_calls:
    result = get_current_time_with_timezone.invoke(call["args"])
    print(f"실행: {call['name']}({call['args']}) [id={call['id']}] -> {result}")
    # tool_call_id가 있어야 모델이 "이 결과가 어느 호출에 대한 응답인지" 알 수 있습니다.
    messages3.append(ToolMessage(content=str(result), tool_call_id=call["id"]))

final3 = model_with_tools3.invoke(messages3)
print("\n=== 최종 답변 (서울·뉴욕 모두 반영되어야 함) ===")
print(final3.content)
```

**[체크포인트]** 최종 답변에 서울 시각과 뉴욕 시각이 **모두** 들어 있나요? 만약
`ToolMessage`의 `tool_call_id`를 실수로 하나만 넣거나 잘못된 `id`를 넣으면 어떻게
되는지 한 번 일부러 틀리게 만들어 실행해 보세요 (도전 과제로 남겨도 좋습니다).

> 💡 **이 셀들이 왜 중요한가**: 방금 손으로 한 줄씩 짜본 이 과정 —
> **"`tool_calls`를 순회하며(for) 각각 실행하고, 결과를 `id`로 짝지어 `ToolMessage`로
> 돌려준다"** — 가 바로 A-6 `multiturn_chatbot.py`의 핵심 루프와 정확히 같은
> 동작입니다.

```python
# A-6에서 다시 만나게 될 코드
tool_map = {
"get_current_time_with_timezone": get_current_time_with_timezone,
"word_count": word_count,
"calculator": calculator
}

if ai_message3.tool_calls:
    for call in ai_message3.tool_calls:
        tool_fn = tool_map[call["name"]]
        result = tool_fn.invoke(call["args"])
        print(result)
        messages3.append(ToolMessage(content=str(result), tool_call_id=call["id"]))

messages3
```

> 차이는 딱 두 가지입니다. ① 오늘은 `get_current_time_with_timezone.invoke(...)`처럼
> 도구 이름을 코드에 직접 썼지만, A-6에서는 `tool_map[call["name"]]`으로 **어떤 도구인지
> 이름을 보고 찾아서** 실행합니다(도구가 여러 개일 때 자동으로 골라 쓰기 위해서입니다).
> ② 오늘은 호출이 한 번만 있으면 끝났지만, A-6은 `while`로 감싸서 **모델이 도구를 더
> 요청하지 않을 때까지 계속** 반복합니다. 방금 손으로 확인한 "여러 개의 `tool_calls`를
> `for`로 순회하고 `id`로 결과를 짝짓는다"는 동작 원리는 그대로입니다.

**[도전 과제]**
1. `get_current_time_with_timezone`의 docstring에서 예시 도시를 지우고 "시간대를 받아
   현재 시간을 알려준다"처럼 애매하게 바꿔보세요. 모델이 `timezone` 인자에 "뉴욕",
   "New York" 같은 잘못된 형식을 넣어 `시간대 오류`가 나는지 관찰해 보세요.
2. "서울, 뉴욕, 런던 지금 몇 시야?"처럼 도시를 3개로 늘려서 `tool_calls`가 3개로
   늘어나는지, 셀 19의 `for` 루프가 개수에 상관없이 그대로 동작하는지 확인해 보세요.

### A-5. 유튜브 검색·자막·요약 파이프라인 — `youtube_summary.ipynb` (새 노트북)

A-6에서 유튜브 도구를 챗봇에 싣기 전에, 그 도구 **안에서 벌어지는 일**을 먼저 맨손으로
실행해 봅니다 — 검색(`YoutubeSearch`) → URL 조립 → 자막 로드(`YoutubeLoader`) →
LCEL 체인으로 요약. A-3에서 `response` 구조를 손으로 뜯어본 것과 같은 이유입니다:
**자동화하기 전에 각 단계를 눈으로 확인**합니다.

`week6-agent` 폴더에 `youtube_summary.ipynb` 노트북을 새로 만들고, 커널은 A-3과 같은
`week6-agent/.venv`를 선택하세요. 패키지를 설치합니다 — 유튜브 검색도 자막도
**API Key가 필요 없습니다.**

```bash
uv add youtube-search youtube-transcript-api langchain-community
```

**셀 1 — 키워드로 유튜브 검색하기**

```python
# YoutubeSearch 패키지로 유튜브 검색 (API Key 불필요)
from youtube_search import YoutubeSearch

videos = YoutubeSearch("생성형 AI 교육 활용", max_results=5).to_dict()
videos
```

결과는 dict의 리스트입니다. 각 영상마다 `id`, `title`, `channel`, `duration`,
`views`, `url_suffix` 같은 키가 들어 있는지 확인하세요.

**셀 2 — 완전한 URL 만들기**

```python
video_url = 'https://youtube.com' + videos[0]['url_suffix']
video_url
```

**셀 3 — YoutubeLoader로 자막 가져오기 (영상 1개)**

```python
# 내부적으로 youtube_transcript_api를 사용합니다
from langchain_community.document_loaders import YoutubeLoader

loader = YoutubeLoader.from_youtube_url(
    video_url,
    language=['ko', 'en']  # 자막 언어 우선순위
)
loader.load()
```

반환값이 5주차 RAG에서 본 것과 같은 **`Document` 객체 리스트**라는 점에 주목하세요 —
`page_content`에 자막 전문이 들어 있습니다. 유튜브 영상도 PDF·웹페이지처럼
"LangChain 문서 로더로 읽는 데이터 소스" 중 하나일 뿐입니다.

**셀 4 — 모든 영상의 자막 가져오기 (실패 대비 포함)**

```python
for v in videos:
    v['video_url'] = 'https://youtube.com' + v['url_suffix']
    try:
        loader = YoutubeLoader.from_youtube_url(v['video_url'], language=['ko', 'en'])
        v['content'] = loader.load()
    except Exception as error:
        v['content'] = []          # 자막이 없는 영상도 있습니다
        v['error'] = str(error)

[(v['title'], len(v['content'])) for v in videos]
```

**셀 5 — 길이가 1시간 이하인 영상만 고르기**

```python
print('총 영상 수:', len(videos))

# duration이 "1:23:45"처럼 세 부분이면 1시간 이상 → 제외
videos = [v for v in videos if len(v['duration'].split(':')) < 3]
print('60분 이하 영상 수:', len(videos))
```

**셀 6 — 요약용 모델 설정**

```python
# 과정 표준 모델로 통일합니다 (원본 예제의 gpt-4o-mini 대신)
from langchain_anthropic import ChatAnthropic

model = ChatAnthropic(model="claude-sonnet-4-6", temperature=0)
model.invoke('안녕?')
```

**셀 7 — 영상 하나 요약하기 (3주차 LCEL 체인 복습)**

```python
from langchain_core.prompts import ChatPromptTemplate
from langchain_core.runnables import RunnablePassthrough

def format_documents(docs):
    """Document 목록에서 page_content만 추출해 하나의 문자열로 결합합니다."""
    return "\n\n".join(doc.page_content for doc in docs)


prompt = ChatPromptTemplate.from_messages(
    [(
        "system",
        "다음 영상에 대한 요약을 한국어로 만들어줘.:\n\n{context}"
    )]
)

summary_chain = (
    RunnablePassthrough.assign(
        context=lambda inputs: format_documents(inputs["context"]),
    )
    | prompt
    | model
)

result = summary_chain.invoke({"context": videos[0]['content']})
result
```

**셀 8 — 요약 출력**

```python
print(result.content)
```

**셀 9 — 모든 영상 요약하기**

```python
from tqdm import tqdm  # tqdm은 진행 상황을 보여주는 라이브러리

for v in tqdm(videos):
    v['summary'] = summary_chain.invoke({"context": v['content']}).content

for v in videos:
    print(f"■ {v['title']} ({v['video_url']})")
    print(v['summary'][:200], "...\n")
```

> ⚠️ **토큰(비용) 주의**: 이 셀은 **영상마다 자막 전문**을 모델에 넣습니다. 자막이 긴
> 영상 5개면 한 번의 실행으로 수만 토큰이 들어갈 수 있습니다 — 내일(8/11) Tavily 실습의
> "스니펫 vs 원문(raw_content)" 비교와 정확히 같은 문제입니다. 그래서 A-6에서 이걸
> 도구로 만들 때는 **영상 수를 3개로 줄이고 자막을 앞 2,000자로 잘라** 넣습니다.

**[체크포인트]**
- [ ] 셀 1의 검색 결과에서 `title`·`duration`·`url_suffix` 키를 확인했나요?
- [ ] 셀 3의 반환값이 RAG에서 본 `Document` 객체라는 것을 확인했나요?
- [ ] 자막이 없는 영상이 있었나요? (셀 4의 `error` 키 확인 — 이게 정상입니다)
- [ ] 셀 9에서 영상별 요약이 생성되었나요?

**[도전 과제]**
1. 검색어를 본인 강의·연구 분야로 바꿔 같은 파이프라인을 돌려 보세요.
2. 요약 프롬프트를 "핵심을 3줄로 요약하고 대상 청중을 추정해줘"로 바꿔 결과를 비교해 보세요.
3. 셀 9를 자막이 있는 영상(`v['content']`가 비어 있지 않은 것)만 요약하도록 고쳐 보세요.

### A-6. 도구 4개를 실은 멀티턴 챗봇 — `multiturn_chatbot.py`

지금까지 노트북에서 **셀 단위로 검증한 재료들을 하나의 대화형 에이전트로 조립**합니다.
챗봇에 싣는 도구는 4개입니다. (A-3의 `calculator`는 구조 학습용이었으므로 챗봇에서는
빼고, 이름은 `get_*` 스타일로 통일합니다.)

| 도구 | 출처 | 역할 |
|---|---|---|
| `get_word_count` | A-3의 `word_count` | 단어·글자 수 세기 |
| `get_current_time_with_timezone` | A-4 | 시간대별 현재 시각 |
| `get_web_search` | 신규 (DuckDuckGo — API Key 불필요) | 웹 최신 정보 검색 |
| `get_youtube_search` | A-5 파이프라인을 함수 하나로 | 유튜브 검색 + **자막까지** 가져오기 |

핵심은 두 가지입니다.

1. **`get_ai_response(messages)`**: 메시지 목록을 받아 모델을 호출하고, 모델이 도구
   호출을 요청하면 그 도구를 실행한 뒤 결과를 다시 메시지에 붙여 모델에게 돌려주는 과정을
   **도구 호출이 더 이상 없을 때까지** 반복하는 함수입니다. A-4에서 손으로 한 줄씩 했던
   확인·실행·재호출을 함수 하나로 캡슐화해 자동으로 돌아가게 만듭니다.
2. **`while True` 루프**: 사용자 입력을 계속 받고, 매번 `messages` 리스트에 대화를
   누적시킵니다. `messages`가 대화 전체를 기억하는 저장소 역할을 하므로, 이전 턴에서
   나온 결과를 다음 턴에서 자연스럽게 참조할 수 있습니다.

DuckDuckGo 검색용 패키지만 추가로 설치합니다 (나머지는 A-5까지 설치 완료).

```bash
uv add duckduckgo-search
```

```python
# multiturn_chatbot.py
import os
from dotenv import load_dotenv
from datetime import datetime
from zoneinfo import ZoneInfo

from langchain_anthropic import ChatAnthropic
from langchain_core.tools import tool
from langchain_core.messages import HumanMessage, SystemMessage, ToolMessage
from langchain_community.tools import DuckDuckGoSearchResults
from langchain_community.utilities import DuckDuckGoSearchAPIWrapper
from langchain_community.document_loaders import YoutubeLoader
from youtube_search import YoutubeSearch

load_dotenv()


@tool
def get_word_count(text: str) -> str:
    """텍스트의 단어 수와 글자 수를 센다."""
    return f"단어 수: {len(text.split())}, 글자 수: {len(text)}"


@tool
def get_current_time_with_timezone(timezone: str) -> str:
    """IANA 시간대 이름(예: 'Asia/Seoul', 'America/New_York')을 받아
    해당 지역의 현재 시간을 알려준다."""
    try:
        now = datetime.now(ZoneInfo(timezone))
        return now.strftime("%Y-%m-%d %H:%M:%S %Z")
    except Exception as e:
        return f"시간대 오류: {e} (예: 'Asia/Seoul', 'America/New_York' 형식으로 입력)"


@tool
def get_web_search(query: str, search_period: str = "m") -> str:
    """웹에서 최신 정보를 검색한다.

    Args:
        query: 검색어
        search_period: 검색 기간 ('d': 하루, 'w': 일주일, 'm': 한 달, 'y': 일 년)
    """
    wrapper = DuckDuckGoSearchAPIWrapper(region="kr-kr", time=search_period)
    search = DuckDuckGoSearchResults(api_wrapper=wrapper, results_separator=";\n")
    return search.invoke(query)


@tool
def get_youtube_search(query: str) -> str:
    """유튜브에서 영상을 검색하고, 각 영상의 자막 내용까지 가져온다.
    영상 추천·비교·내용 요약 요청에 사용한다.

    Args:
        query: 검색어
    """
    videos = YoutubeSearch(query, max_results=3).to_dict()
    videos = [v for v in videos if len(v["duration"].split(":")) < 3]  # 1시간 이상 제외

    results = []
    for v in videos:
        video_url = "https://youtube.com" + v["url_suffix"].split("&")[0]
        try:
            docs = YoutubeLoader.from_youtube_url(video_url, language=["ko", "en"]).load()
            # 토큰 절약: 자막은 영상당 앞 2,000자만 전달한다 (A-5 셀 9의 비용 문제 참고)
            content = " ".join(d.page_content for d in docs)[:2000]
        except Exception as error:
            content = f"(자막을 가져오지 못했습니다: {error})"
        results.append(
            f"제목: {v['title']}\n채널: {v['channel']} | 길이: {v['duration']}\n"
            f"URL: {video_url}\n자막(일부): {content}"
        )
    return "\n\n---\n\n".join(results) if results else "검색 결과가 없습니다."


tools = [
    get_word_count,
    get_current_time_with_timezone,
    get_web_search,
    get_youtube_search,
]
tool_map = {t.name: t for t in tools}

model = ChatAnthropic(model="claude-sonnet-4-6", temperature=0)
model_with_tools = model.bind_tools(tools)


def get_ai_response(messages: list) -> list:
    """모델을 호출하고, 도구 호출이 있으면 실행까지 마친 뒤
    최종 답변까지 포함된 전체 messages를 반환한다."""
    ai_message = model_with_tools.invoke(messages)
    messages.append(ai_message)

    # 모델이 도구 호출을 요청하는 동안 계속 실행한다 (여러 도구를 연쇄 호출할 수도 있음)
    while ai_message.tool_calls:
        for call in ai_message.tool_calls:
            tool_fn = tool_map[call["name"]]
            result = tool_fn.invoke(call["args"])
            messages.append(ToolMessage(content=str(result), tool_call_id=call["id"]))
        ai_message = model_with_tools.invoke(messages)
        messages.append(ai_message)

    return messages


def main():
    print("=== 도구 4개를 가진 멀티턴 챗봇 (종료하려면 'exit' 입력) ===")
    messages = [
        SystemMessage(content=(
            "너는 사용자의 질문에 가장 적합한 도구를 골라 호출해 답하는 AI 조교다. "
            "도구 결과에 없는 내용은 추측하지 마라."
        ))
    ]

    while True:
        user_input = input("\n나: ").strip()
        if user_input.lower() in ("exit", "quit", "종료"):
            print("챗봇을 종료합니다.")
            break
        if not user_input:
            continue

        messages.append(HumanMessage(content=user_input))
        messages = get_ai_response(messages)

        print("에이전트:", messages[-1].content)


if __name__ == "__main__":
    main()
```

```bash
uv run python multiturn_chatbot.py
```

**다양한 실습 프롬프트 예시**

아래 시나리오를 직접 입력해 보면서, 매번 어떤 도구가 선택되는지 관찰해 보세요.

| 시나리오 | 대화 예시 | 확인 포인트 |
|---|---|---|
| ① 시간 질문 | "지금 서울이랑 뉴욕 몇 시야?" | `get_current_time_with_timezone`이 **2번** 호출되는가? (A-4의 다중 호출) |
| ② 웹 검색 | "이번 주 생성형 AI 관련 뉴스 검색해줘" | `get_web_search`가 호출되고 출처가 표시되는가? |
| ③ 유튜브 검색·요약 | "LangGraph 입문 강의 영상 찾아서 내용 요약해줘" | `get_youtube_search` 한 번으로 검색+자막이 오고, 모델이 요약하는가? |
| ④ 도구 전환 멀티턴 | (③에 이어) "방금 그 요약의 단어 수 세어줘" | 이전 턴의 요약을 기억한 채 `get_word_count`로 전환되는가? |
| ⑤ 도구 필요 없는 턴 | "안녕!" → "고마워!" | 도구 호출 없이 바로 답하는가? |
| ⑥ 존재하지 않는 요청 | "내 이메일함 좀 확인해줘" | 도구가 없다고 정직하게 답하는가? |

**[체크포인트]**
- [ ] 시나리오 ①에서 `tool_calls`가 2개 생기고 두 도시가 모두 답변에 반영되나요?
- [ ] 시나리오 ③에서 유튜브 **검색과 자막**이 도구 호출 한 번에 함께 오나요?
- [ ] 시나리오 ④에서 모델이 이전 턴의 결과를 다시 계산하지 않고 **기억한 채로** 이어가나요?
- [ ] `exit`을 입력하면 루프가 정상적으로 끝나나요?

**[도전 과제]**
1. `while ai_message.tool_calls:` 루프에 반복 횟수 상한(예: 5회)을 두어, 도구 호출이
   비정상적으로 반복될 때 안전하게 멈추도록 고쳐보세요.
2. `get_youtube_search`의 자막 절단 길이(2,000자)를 500자/5,000자로 바꿔 보며, 답변 품질과
   토큰 사용량(비용)이 어떻게 달라지는지 관찰해 보세요.
3. `messages`가 너무 길어지면 어떻게 될지 생각해 보세요. 3주차에 배운 토큰 개념을
   떠올리며, "대화가 길어질수록 매 턴 비용이 늘어난다"는 것을 직접 관찰해 보세요.

> 💡 **왜 지금 이 실습을 하는가**: `messages` 리스트를 직접 손으로 관리하며 대화를
> 이어가는 이 경험이, 바로 내일 배울 LangGraph의 **State** 개념과 정확히
> 같은 역할을 합니다. LangGraph는 이 `messages` 누적과 도구 호출 반복을 그래프
> 구조(`add_messages`, 조건부 엣지)로 자동화해 주는 것뿐입니다 — 즉 오늘 손으로
> 짠 반복문을, 내일(8/11) LangGraph 세션에서는 그래프가 대신 돌려줍니다.

> 🔭 **7주차 예고**: 이 챗봇의 도구들은 7주차 1일차에서 **MCP 서버로 이사**합니다.
> 특히 `get_youtube_search`는 검색 전용으로 다이어트한 뒤 공개 MCP의 자막 도구와
> 연쇄시키는 실습으로 이어지고, 터미널 대신 **웹 채팅 UI**(Gradio)도 그때 붙입니다.
---
### 도전 과제

다음 Tool 가운데 본인의 교수 업무에 필요한 하나를 추가한다.

- 강의 주차 계산 Tool
- 점수 구간 판정 Tool
- 강의계획서 항목 검사 Tool
- 참고문헌 형식 검사 Tool
- 회의록 항목 수 집계 Tool

Tool 설계표:

| 항목 | 작성 내용 |
|---|---|
| Tool 이름 | |
| 해결할 문제 | |
| 입력값 | |
| 반환값 | |
| 사용 조건 | |
| 오류 입력 예 | |
| 오류 처리 방식 | |
---

## 세션 4 — Memory 연동

### 4-1. mem0 — "대화가 끝나도 남는 기억"

mem0는 **사용자별 사실(fact)만 뽑아 저장**하고, 다음 대화에서 관련된 것만 검색해 불러옵니다.

| 구분 | 방식 |
|---|---|
| RunnableWithMessageHistory (3주차) | 이번 대화의 메시지를 통째로 기억 (세션이 끝나면 사라짐) |
| mem0 (오늘) | "이 학생은 통계학을 어려워한다" 같은 **사실**을 장기 저장, 다음 세션에도 유지 |

> 🔁 지금까지 다룬 Tool Use가 "필요한 순간에 행동하는 능력"이었다면, mem0는
> **"오늘 대화가 끝나도 잊지 않는 능력"**입니다. A-6 멀티턴 챗봇의 `messages` 리스트는
> **이번 대화 세션에서만** 기억이 유지되지만, mem0는 프로그램을 껐다 켜도, 심지어 며칠이
> 지나도 저장해 둔 사실을 다시 불러올 수 있습니다.

### B-1. mem0 연동 질의 — `memory_demo.ipynb`

**셀 1 — 라이브러리와  API Key 불러오기(Anthropic & Openai API key 모두 필요 )**
```python
# memory_demo.ipynb
import os
from dotenv import load_dotenv
from mem0 import Memory
os.environ["MEM0_TELEMETRY"] = "false"

load_dotenv()
```
**셀 2 — Memory 설정**
```python
## Claude 사용하는 경우
# config = {
#     "llm": {
#         "provider": "anthropic",
#         "config": {
#             "model": "claude-sonnet-4-6",

#         }
#     }
# }

## OpenAI 모델을 사용하는 경우
config = {
    "llm": {
        "provider": "openai",
        "config": {
            "model": "gpt-5-mini",
            "is_reasoning_model": True
        }
    }
}
memory = Memory.from_config(config)
```
**셀 2 — Memory에 정보 저장**
```python
user_id = "professor_kim"

memory.add("이 학생은 통계학 기초를 특히 어려워한다", user_id=user_id)
memory.add("2학년 전공필수 수업을 담당한다", user_id=user_id)

results = memory.search("이 학생에게 어떤 자료를 추천할까?", user_id=user_id)
for r in results["results"]:
    print("-", r["memory"])
```

**셀 3 — Memory에 정보 저장**
```bash
 (가상환경 안에서는  uv pip install mem0ai)
uv run python memory_demo.py
```

**[체크포인트]** 다음의 결과가 나오면 맞습니다.
> - User's student finds the fundamentals of statistics particularly difficult.
> - User teaches a second-year required major course.

**셀 4 — Memory에 정보를 활용한 질의**
```python

from langchain.chat_models import init_chat_model
from langchain_core.messages import SystemMessage, HumanMessage

llm = init_chat_model("anthropic:claude-sonnet-4-6",
        temperature= 0.2)

def answer_with_memory(question: str, user_id: str) -> str:
    # 1. 질문과 관련된 기억 검색
    search_result = memory.search(
        question,
        filters={"user_id": user_id},
        top_k=5,
    )

    retrieved_memories = [
        item["memory"]
        for item in search_result.get("results", [])
    ]

    # 2. 검색 결과를 모델이 읽을 수 있는 문맥으로 변환
    if retrieved_memories:
        memory_context = "\n".join(
            f"- {item}" for item in retrieved_memories
        )
    else:
        memory_context = "- 관련 기억 없음"

    # 3. 기억을 참고해 사용자 질문에 답변
    system_prompt =  f"""
        당신은 대학 교수의 수업 설계를 돕는 교육 조교입니다.

        아래 기억은 사용자와 관련된 참고 정보입니다.
        질문에 관련된 내용만 활용하고, 기억에 없는 사실은 임의로 만들지 마세요.

        [관련 기억]
        {memory_context}
    """.strip()

    response = llm.invoke([
        SystemMessage(content= system_prompt),
        HumanMessage(content=question)
        ]
    )
    answer = response.text

    # 4. 이번 대화도 다음 질문에서 활용할 수 있도록 저장
    memory.add(
        [
            {"role": "user", "content": question},
            {"role": "assistant", "content": answer},
        ],
        user_id=user_id,
    )

    return answer

question = "이 학생에게 어떤 학습 자료를 추천하면 좋을까?"
answer = answer_with_memory(question, user_id)

print("\n[최종 답변]")
print(answer)
```
**[체크포인트]** 검색 결과에 "통계학 기초"와 관련된 기억이 나오나요? (전공 담당 사실은
관련성이 낮아 안 나올 수도 있습니다 — 이게 정상입니다. 관련된 것만 골라 오는 게 mem0의 역할)

**[도전 과제]** `user_id`를 다른 값(예: `"professor_lee"`)으로 바꿔서 같은 코드를
다시 실행해 보세요. 방금 저장한 기억이 검색되지 않는 것을 확인하고, `user_id`가
사용자별 기억을 어떻게 분리하는지 체감해 보세요.


---

## 오늘의 산출물 체크

- [ ] `tool_basics.ipynb` — 계산기·단어수·시간 Tool을 셀 단위로 쌓으며 `response` 구조
      (tool_calls/content/stop_reason/usage_metadata)와 다중 도구 호출을 분석한 결과
- [ ] `youtube_summary.ipynb` — 유튜브 검색 → 자막(Document) → LCEL 요약 파이프라인 실행 결과
- [ ] `multiturn_chatbot.py` — 도구 4개(`get_word_count`·`get_current_time_with_timezone`·
      `get_web_search`·`get_youtube_search`)를 실은 멀티턴 챗봇
- [ ] `memory_demo.ipynb` — mem0로 사용자별 사실 저장·검색 결과
- [ ] 아래 템플릿으로 `W6_D1_이름.md` 작성

## 제출용 Markdown 템플릿

```markdown
# AI MASTER 6주차 1일차 — [이름 / 학과]

## 1. Tool Use
- 등록한 Tool 목록 및 각 도구의 용도:
- 모델이 도구를 잘못 고른 사례가 있었나:
- (A-3) `response`에서 확인한 `stop_reason`(tool_use/end_turn) 차이:
- (A-4) 시간대 없는 Tool이 오답을 낸 사례, 그리고 다중 도구 호출(`tool_calls` 2개 이상)을 직접 확인한 결과:
- (A-5) 유튜브 검색·자막·요약 파이프라인 실행 결과 (자막 없는 영상 처리 포함):
- (A-6) 멀티턴 대화에서 도구 전환·이전 턴 기억을 확인한 사례:

## 2. Memory (mem0)
- 저장한 사실 예시 2개:
- 검색 시 관련 없는 기억이 걸러졌는가:
- `user_id`를 바꿔서 기억이 분리되는 것을 확인했는가:

## 3. 본인 에이전트 적용 계획
- 3~5주차 조교/RAG에 오늘 배운 Tool Use·Memory 중 무엇을 먼저 붙일 것인가:
```

---

## 자주 막히는 지점 (트러블슈팅)

- **`uv add`가 버전 충돌을 냅니다** → `uv add langgraph langchain-anthropic` 순서로 핵심
  패키지를 먼저 넣고, 이후 패키지는 그 다음에 추가하세요. 그래도 안 되면 `uv sync --upgrade`.
- **API 호출 비용이 걱정돼요** → 오늘 실습은 질문 1건당 Tool 호출 포함 2~4회 API 호출 정도로,
  전체 실습 비용은 크지 않습니다. 반복 테스트할 때는 `temperature=0`으로 재현성을 높이세요.
  단, A-5 셀 9(전체 영상 요약)와 A-6의 유튜브 도구는 자막 길이만큼 토큰이 늘어나니
  절단 길이를 확인하세요.
- **`jupyter lab`이 실행되지 않거나 커널을 못 찾아요** → `uv add ipykernel jupyter`가
  먼저 실행되었는지 확인하고, VS Code에서 연다면 우측 상단 "Select Kernel"에서 이
  프로젝트의 `week6-agent/.venv`를 직접 선택하세요. 목록에 안 보이면 `Ctrl+Shift+P` →
  "Developer: Reload Window" 후 다시 시도하거나, "Enter interpreter path..."로
  `.venv/bin/python` 경로를 직접 입력하세요.
- **`response.tool_calls`가 항상 비어 있어요(A-3)** → `model.bind_tools([...])`로 도구를
  바인딩한 `model_with_tools`를 호출했는지 확인하세요. 바인딩 전 `model`을 그대로 호출하면
  도구 호출 자체가 발생하지 않습니다.
- **`ZoneInfoNotFoundError`가 나요(A-4)** → 특히 Windows에서 자주 발생합니다. `uv add
  tzdata`를 실행했는지 확인하세요. 그래도 안 되면 `timezone` 문자열의 대소문자와 슬래시
  표기(`Asia/Seoul`, `America/New_York`)가 정확한지 확인하세요.
- **"서울, 뉴욕" 질문에서 결과가 하나만 나와요(A-4)** → `for call in
  ai_message3.tool_calls:` 루프 안에서 `messages3.append(ToolMessage(...))`를 매 호출마다
  하고 있는지 확인하세요. 한 호출의 `ToolMessage`만 추가하고 최종 호출을 하면, 모델이
  나머지 도구 결과를 받지 못해 답변에서 빠뜨릴 수 있습니다.
- **유튜브 검색·자막이 안 돼요(A-5·A-6)** → ① 학교·회사 네트워크가 유튜브를 차단하면
  실패합니다 — 개인 네트워크에서 시도하세요. ② 자막이 아예 없는 영상은 `YoutubeLoader`가
  예외를 냅니다 — A-5 셀 4·A-6 도구의 `try/except`가 처리하는지 확인하세요.
  ③ `youtube-search` 패키지는 유튜브 화면을 읽는 방식이라 유튜브 개편 시 깨질 수 있습니다 —
  그때는 `yt-dlp`(`ytsearchN:검색어`)로 검색 부분만 교체하면 됩니다.
- **DuckDuckGo 검색 결과가 비거나 Ratelimit 오류가 나요(A-6)** → 잠시 후 다시 시도하세요.
  패키지 이름 변경 안내가 나오면 `uv add ddgs`로 새 패키지를 설치합니다.
- **`multiturn_chatbot.py`가 도구 호출을 반복하며 멈추지 않아요** → `get_ai_response`의
  `while ai_message.tool_calls:` 루프에 반복 횟수 상한을 두지 않으면 이론적으로 계속
  돌 수 있습니다. [도전 과제 1]의 상한 로직을 꼭 추가하세요.
- **`input()`이 동작하지 않거나 멈춰요** → Jupyter Notebook 등 일부 환경에서는 `input()`이
  제대로 동작하지 않을 수 있습니다. 터미널에서 `uv run python multiturn_chatbot.py`로
  직접 실행하세요.
- **`mem0` 설치가 무겁거나 OpenAI 키가 없어요** → mem0는 기본적으로 OpenAI 임베딩을 쓰지만,
  `Memory.from_config()`에 로컬 임베딩(예: `sentence-transformers`) 설정을 넣어 대체할 수
  있습니다. 오늘은 우선 OpenAI 키로 진행하고, 안 되면 강사에게 로컬 설정 스니펫을 요청하세요.
- **`memory.search()` 결과가 하나도 안 나와요** → 방금 `memory.add()`한 직후라면 인덱싱에
  잠깐 시간이 걸릴 수 있습니다. 몇 초 후 다시 검색해 보거나, `user_id`가 `add`와 `search`에서
  정확히 같은 문자열인지 확인하세요.
